# LaTeX templates are read verbatim and asserted on byte-for-byte in tests, so their
# line endings must not depend on the checkout. core.autocrlf=true was rewriting them
# to CRLF and breaking LatexRendererTest on Windows.
*.tex text eol=lf
